export interface Album {
  id: number;
  author: string;
  title: string;
  url: string;
  download_url: string;
}

export interface Photo {
  id: number;
  author: string;
  url: string;
  download_url: string;
}
